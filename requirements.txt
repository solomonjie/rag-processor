# 直接依赖（保持精简；间接依赖由 pip 自行解析，版本与 rag_env 实测对齐）
trafilatura>=2.0
pandas==2.3.3          # xlsx 输入读取
openpyxl==3.1.5        # pandas 的 xlsx 引擎
minio==7.2.20          # 批次源对象存储（可选，配置 Minio_Endpoint 后启用）
llama-index-core==0.14.15
llama-index-embeddings-text-embeddings-inference==0.4.2
llama-index-vector-stores-milvus==1.0.0
pymilvus==2.6.9
openai==2.26.0
python-dotenv==1.2.2
